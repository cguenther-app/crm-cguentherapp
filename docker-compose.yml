version: '3.8'

services:
  crm-pocketbase:
    image: ghcr.io/muchobien/pocketbase:latest
    container_name: crm-pocketbase
    restart: unless-stopped
    volumes:
      - ./pocketbase/pb_data:/pb_data
    ports:
      - "127.0.0.1:8090:8090"
    networks:
      - crm-internal

  crm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_POCKETBASE_URL=https://crm.cguenther.app/pb
    container_name: crm-frontend
    restart: unless-stopped
    environment:
      - POCKETBASE_URL=http://crm-pocketbase:8090
      - NEXT_PUBLIC_POCKETBASE_URL=https://crm.cguenther.app/pb
    ports:
      - "3100:3000"
    depends_on:
      - crm-pocketbase
    networks:
      - crm-internal

networks:
  crm-internal:
    driver: bridge
